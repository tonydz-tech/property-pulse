DATABASE_URL="postgresql://user@localhost:5432/property_pulse_dev?schema=public"
# In production (Supabase/Neon), DATABASE_URL should be the pooled connection
# and DIRECT_URL the direct connection (used only for running migrations).
DIRECT_URL="postgresql://user@localhost:5432/property_pulse_dev?schema=public"

# Generate with: npx auth secret
AUTH_SECRET=""
